# Credenciais dummy do LocalStack — nunca são reais, mas seguem o mesmo
# mecanismo de injeção por variável de ambiente que seria usado com AWS
# real (nunca hardcoded no .tf, ver ADR-001).
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1
