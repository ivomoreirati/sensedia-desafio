services:
  localstack:
    image: localstack/localstack:3.0
    ports:
      - "4566:4566"
    environment:
      - SERVICES=lambda,dynamodb,iam,sts,logs
      - DEBUG=0
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
